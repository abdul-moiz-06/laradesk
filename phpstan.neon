includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: max
    paths:
        - app
        - database
        - routes
    excludePaths:
        # Vendor-published migration (spatie/laravel-permission) — third-party generated code.
        - database/migrations/*_create_permission_tables.php
    tmpDir: build/phpstan
